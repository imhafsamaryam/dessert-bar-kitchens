import React from 'react'

const Button = ({text}) => {
  return (
    <button  className='bg-[#C5B5A6] px-[1rem] pt-[0.4rem] pb-[0.5rem] rounded-3xl w-max'>
      {text}
    </button>
  )
}

export default Button
